package main

func create(mess Message) error {
	//llamar a su bd
	/*bd, err := getDB()
	if err != nil {
		return err
	}
	_, err = bd.Exec("INSERT INTO table1 (name, year) VALUES (?, ?)", p.Name, p.Year)
	return err*/
	Array[1]=mess.Name;
	return nil;
}
func get()([2]string,error){
	//llamar a su db

	return Array, nil
}